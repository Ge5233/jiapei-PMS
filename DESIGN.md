# DESIGN.md — 产品管理系统（PMS）

## 项目定位
- 内部产品资料 + 价格管理后台
- 用户规模：3-4 人
- 形态：单页 Web 后台，浏览器访问
- 风格基调：**专业、克制、易读**，后台工具属性强

## Design Tokens

### 色彩
- **主色**：`#2563eb`（蓝）— 链接、按钮、激活状态
- **辅助色**：`#10b981`（绿，毛利率健康）/`#f59e0b`（黄，毛利率偏低）/`#ef4444`（红，毛利率警告 / 突破折扣）
- **中性色**：`#0f172a`（标题）/`#475569`（正文）/`#94a3b8`（次要文字）/`#e2e8f0`（分隔线）/`#f8fafc`（背景）
- **背景**：白底（`#ffffff`），浅灰分区（`#f8fafc`）
- 意象来源：财务/ERP 类工具的冷静蓝，避免"消费互联网"的鲜艳

### 字体
- 中文：`PingFang SC` / `Microsoft YaHei` / 思源黑体（系统自带兜底）
- 英文/数字：`SF Pro` / `Inter` / `system-ui`（便于等宽对齐价格）
- 价格字段用 `font-variant-numeric: tabular-nums`，等宽数字，列表对齐好看

### 间距与圆角
- 页面 padding：`24px`
- 卡片间距：`16px`
- 按钮/输入框圆角：`6px`（避免太圆显得不专业）
- 卡片圆角：`8px`

### 动效
- 页面切换 / 弹窗：`150ms ease-out`
- 不用复杂动效，以"反馈即时"为主

## 布局与结构

### 整体
- 左侧菜单（固定 220px）+ 顶部条（高 56px）+ 主内容区
- 左侧菜单分组：**业务**（首页/产品/分类/报价） / **系统**（用户/我的资料）
- 角色：管理员可见"用户"菜单

### 列表页
- 顶部：标题 + 主操作按钮（"新增产品"）
- 筛选条：搜索 + 分类级联 + 状态 + 导出 Excel
- 表格：斑马纹、hover 高亮、操作列右对齐
- 分页：底部居中

### 表单页
- 单列布局，最大宽度 720px
- 分组卡片：基本信息 / 价格信息 / 资料文件
- 价格字段聚焦：进价/售价/折扣 三个字段并排 + 实时显示毛利率

## 组件规范

### 按钮
- 主按钮：`bg-blue-600 text-white hover:bg-blue-700`
- 次按钮：`bg-white text-slate-700 border border-slate-300`
- 危险按钮：`bg-red-600 text-white`（删除用）
- 尺寸：`h-9 px-4`（中等）

### 标签 / 徽章
- 状态徽章：上架绿 / 下架灰
- 角色徽章：管理员紫 / 员工蓝
- 毛利率徽章：按数值红/黄/绿

### 价格展示
- 数字右对齐，等宽
- 单位"¥"小一号字
- 毛利率紧随其后，括号 `(12.5%)`

## 交互

- 删除前确认弹窗（防误操作）
- 价格修改保存后**自动写入历史**（用户感知不到，但详情页可看）
- 拖拽分类：分类项出现拖拽手柄，hover 时光标变 `move`
- 上传文件：拖拽上传区 + 进度条

## 设计禁忌

- ❌ 不要用渐变色背景（管理后台不需要花哨）
- ❌ 不要用 emoji 当 UI 图标（统一用 Lucide icon 或纯 SVG）
- ❌ 不要做暗色模式（3-4 人用，开销不值得）
- ❌ 不要给产品加封面图（产品图通过"产品资料"上传）
- ❌ 不要用大字号大留白（这是工具，不是营销页）

## 文案

- 中文优先，简洁直白
- 按钮文字用动词（"保存"、"删除"、"导出"），不用"确定提交"
- 错误提示要说人话（"数据库连接失败，请检查账号密码"）而非"SQLSTATE[HY000]"
